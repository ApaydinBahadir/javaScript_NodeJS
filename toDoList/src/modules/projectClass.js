import { createHTMLElement } from "./DOMController"

function projectsClass(gTitle, gDescrip, gDueDate, gPriorty, gChecklist){
    var todos = []

    function todoPrint(){
        
    }

    return { todos }
}

export { projectsClass }